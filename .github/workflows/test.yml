name: test

on:
  push:
    branches: [ main ]
  pull_request:

permissions: read-all

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v3
      with:
        node-version: 20
        cache: npm
    - run: npm ci --ignore-scripts
    - run: npm run build
    - run: npm test -w wdio-cjs
    - run: npm test -w wdio-esm
    - run: npm test -w wdio-features
    - run: npm run lint:ci
    - uses: github/codeql-action/upload-sarif@v2
      with:
        sarif_file: eslint.sarif
